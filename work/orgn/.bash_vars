#!/bin/bash

export CDPATH=".:$HOME:$HOME/dev/_metriks:$HOME/dev"

export PATH="/snap/bin:$PATH"

export PATH="$HOME/scripts:$HOME/bin:$HOME/.local/bin:/ke/scripts:/ke/bin:/ke/lib/bash/:$PATH"
export PATH=$PATH:${HOME}/.local/TreeTagger/cmd:${HOME}/.local/TreeTagger/bin

# Uncommented for now as is time consuming each time a bash start
#if which yarn > /dev/null 2>&1 ; then
#	export PATH="$PATH:$(yarn global bin)"
#fi

export TERM=xterm-256color

#export PAGER="most -u"

export LANG=fr_FR.utf8
export TMOUT=0

export DEBEMAIL=orobardet.ext@orange.com
export EDITOR="vim"
export LESSOPEN="| /usr/share/source-highlight/src-hilite-lesspipe.sh %s"
export LESS=" -R "

export GOROOT=/opt/go
export GOPATH=${HOME}/dev/go
export GOTESTCMD="grc go test"
export PATH=$PATH:${GOPATH}/bin:${GOROOT}/bin

export NODE_EXTRA_CA_CERTS="/etc/ssl/certs/ca-certificates.crt"

export SNYK_API=https://snyk.nd-int-ops-paas.itn.intraorange/api
export SNYK_TOKEN=fc691a92-f910-428e-b8f8-54f4b5404288

export NVM_DIR="$HOME/.nvm"                                                                                         
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" --no-use  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

